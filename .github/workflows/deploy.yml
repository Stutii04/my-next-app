name: Deploy to Portainer

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Authenticate to Portainer
        run: |
          set -e
          echo "🔍 Debug: PORTAINER_URL=http://147.93.131.37:9000"
          echo "🔍 Debug: Username=admin"

          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "http://147.93.131.37:9000/api/auth" \
            -H "Content-Type: application/json" \
            -d "{\"username\":\"admin\",\"password\":\"Yd5CwgeBSaCXSbB\"}")

          BODY=$(echo "$RESPONSE" | head -n1)
          STATUS=$(echo "$RESPONSE" | tail -n1)

          echo "🔍 Raw response:"
          echo "$RESPONSE"

          echo "Status: $STATUS"
          echo "Body: $BODY"

          if [ "$STATUS" -ne 200 ]; then
            echo "❌ Authentication failed"
            exit 1
          fi

          JWT=$(echo "$BODY" | jq -r '.jwt')
          echo "✅ Authentication successful, JWT acquired"
          echo "JWT=$JWT" >> $GITHUB_ENV
